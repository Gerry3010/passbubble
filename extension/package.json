{
  "name": "@passbubble/extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:chrome": "vite build --config vite.config.ts && vite build --config vite.config.content.ts && node scripts/build.js chrome",
    "build:firefox": "vite build --config vite.config.firefox.ts && vite build --config vite.config.content.firefox.ts && node scripts/build.js firefox",
    "build": "npm run build:chrome && npm run build:firefox",
    "dev:chrome": "vite build --watch --config vite.config.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@passbubble/shared-ts": "file:../packages/shared-ts",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "webextension-polyfill": "^0.12.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/chrome": "^0.0.330",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@types/webextension-polyfill": "^0.12.0",
    "@vitejs/plugin-react": "^4.5.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vitest": "^3.2.0"
  }
}
